# Backend Environment Variables Example
# Copy this file to .env and fill in your values

# Server Configuration
# Development: NODE_ENV=development, PORT=5000
# Production: NODE_ENV=production, PORT=8080 (Cloud Run default)
NODE_ENV=development
PORT=5000

# Frontend URL (used for CORS and share link generation)
# Development: http://localhost:3000
# Production: https://video-research-40c4b.web.app (your Firebase Hosting URL)
FRONTEND_URL=http://localhost:3000

# Additional frontend URLs for CORS (comma-separated, for production)
# Example: FRONTEND_URLS=https://video-research-40c4b.firebaseapp.com,https://video-research-40c4b.web.app
FRONTEND_URLS=

# Share Feature Configuration (Phase 4)
# FRONTEND_URL is used to generate share links (e.g., ${FRONTEND_URL}/shared/[shareId])
# Development: http://localhost:3000/shared/[shareId]
# Production: https://video-research-40c4b.web.app/shared/[shareId]

# Authentication
AUTH_ENABLED=false
USE_FIREBASE_AUTH=false

# Development User Configuration (for local dev when AUTH_ENABLED=false)
# These values should match the frontend NEXT_PUBLIC_DEV_USER_* values
DEV_USER_ID=dev-user-id
DEV_USER_EMAIL=dev@example.com
DEV_USER_NAME=Development User

# Google OAuth (Only required if AUTH_ENABLED=true and USE_FIREBASE_AUTH=false)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=
JWT_SECRET=

# Firebase Firestore
# Path to service account JSON file (relative to project root or absolute path)
GOOGLE_APPLICATION_CREDENTIALS=./video-research-40c4b-firebase-adminsdk-fbsvc-8ec5f10735.json
# For local development with emulator (optional)
# FIRESTORE_EMULATOR_HOST=localhost:8080

# External APIs (Required)
SUPADATA_API_KEY=your-supadata-api-key
DASHSCOPE_BEIJING_API_KEY=your-dashscope-api-key
# YouTube Data API keys (comma-separated, will be rotated on failure)
YOUTUBE_API_KEYS=key1,key2,key3

# Local Development Configuration
# Use local file storage instead of Firestore (for testing/development)
USE_LOCAL_STORAGE=false

# Optional
REDIS_URL=
LOG_LEVEL=info

# Phase 5: Share Feature Configuration
# Rate limiting for share creation (per user)
SHARE_RATE_LIMIT_PER_USER=10
SHARE_RATE_LIMIT_WINDOW_HOURS=1

# Rate limiting for share access (per IP)
SHARE_ACCESS_RATE_LIMIT_PER_IP=100
SHARE_ACCESS_RATE_LIMIT_WINDOW_HOURS=1

# Abuse detection threshold (accesses per window)
SHARE_ABUSE_DETECTION_THRESHOLD=1000
SHARE_ABUSE_DETECTION_WINDOW_HOURS=1# Cache configuration for frequently accessed shares
SHARE_CACHE_TTL_SECONDS=300

# Enable CAPTCHA for high-frequency shares (optional)
SHARE_ENABLE_CAPTCHA=false